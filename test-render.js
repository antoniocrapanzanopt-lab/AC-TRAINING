import React from 'react';
import ReactDOMServer from 'react-dom/server';
import { AthleteDetailPage } from './src/pages/athletes/AthleteDetailPage';
// We need babel or something to compile TSX to run it directly, or we can use ts-node.
